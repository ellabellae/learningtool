# Design: learningtool — Papers as Lessons

Generated by /office-hours on 2026-09-12; revised by /plan-eng-review and /plan-design-review on 2026-09-13
Branch: main
Repo: ellabellae/learningtool (github.com/ellabellae/learningtool, empty at session start)
Status: APPROVED
Mode: Builder

## Problem Statement

Ella reads research papers for fun across many fields (EEG neurofeedback and BCI in Zotero today; economics and ML too) and finds them "so hard to digest." She wants to drop in a PDF and get back something that makes her *understand* the paper, not a summary. Her words: "I don't want a summary. I want it to be like a learning lesson. What is ChatGPT? What are LLMs? What was GPT-3.5? I go through understanding these concepts and then can really understand GPT-4 and the main takeaways. Think of it like a story." Breadth over depth: "not getting really smart about one thing, it is taking hard material to understand and making it digestible."

## What Makes This Cool

- **A paper becomes a story you play through.** Prerequisite rungs first (the concepts the paper assumes that you lack), then the paper as a plot: the world before, what they tried, *your prediction*, what they found, what changed.
- **You guess the ending.** Before the key result, the lesson asks what you think happened. Then the paper's real finding is revealed. That is retrieval practice without a tutor chat.
- **Sliders on the paper's real knobs.** For the 1-3 parameters that actually change the outcome, a widget lets you "change a variable and see what breaks." Every widget is labeled as an illustrative model and sits beside the paper's own sentence about that mechanism.
- **The paper's exact words, one click away.** Every finding points at an extracted sentence with a page number and a highlighted crop of the actual page. The model never writes the quote; it points at it.
- **It remembers what you've read.** Each lesson leaves concept records, marked read when you open the lesson. The next lesson says "you saw this idea in *Connectome-based neurofeedback* (read 2026-03-03), applied there to attention networks."

## Constraints

- Input is PDF with a text layer. Scanned PDFs are out of scope: `extract` fails fast (mean chars per page < 200) with "no text layer; OCR unsupported" and a non-zero exit.
- Personal tool first. Shareable by cloning the repo plus a profile file. No accounts. Requires `ANTHROPIC_API_KEY`; the README says so.
- LLM calls go through the Anthropic Python SDK with structured output (`messages.parse()` with a Pydantic model; the SDK strips unsupported schema features and validates the response). `claude -p` is rejected because it cannot enforce the schema the checker relies on.
- Files are truth (same pattern as Ella's Atlas tool): profile, spans, lessons, checks, and concept memory are plain JSON/YAML under `data/`. `data/papers/*/source.pdf`, `crops/`, and `lesson.html` (which embeds the crops and runs to several MB) are gitignored, so copyrighted page images never enter the repo; `spans.json`, `lesson.json`, `checks.json` are committed.
- Node 20+ is a runtime requirement, not just a test dependency: `check.py` evaluates widget models through one `node` subprocess per lesson and errors clearly if `node` is missing.
- Papers above 150k characters of span text are refused by `generate` with a message naming the count and "chunking arrives in v1.1". A known v1 limit.
- A lesson is one self-contained HTML file. It cannot write back to the repo. Anything the player records (your prediction) lives in the browser's localStorage and does not feed memory in v1.
- Untrusted input boundaries: Jinja runs with `autoescape=True`; only the inlined widget bundle and CSS are marked safe. The system prompt states that span text is quoted source material, never instructions.
- Git discipline: feature branches, small commits, merges to main, so every change is traceable.
- Tooling on this Mac: Python 3.12, Node 24, bun. PyMuPDF, pymupdf4llm, Pydantic, and the Anthropic SDK are not installed yet; no poppler.
- Generation is one LLM call (2-4 minutes), an audit call, and at most one repair call plus re-audit; worst case about 8 minutes. The stable prompt prefix (spans, registry, memory slugs) is marked cacheable so the audit and repair calls reuse it.

## Premises (agreed)

1. **Breadth over mastery.** Success = "I get what this paper did, why, and what the key knobs are" in 10-15 minutes. Recall and testing are optional deep-mode extras.
2. **The unit of output is a lesson, not a page.** Prerequisite rungs built from the profile, then story scenes: world before + problem, what they tried, your prediction, what they found, what changed + takeaways. Each scene is one screen with an analogy from things you know, an optional widget, and a "paper's words" control. A depth dial (brief / standard / deep) controls how many rungs and scenes you see. *Revised twice in office hours: from "one interactive page" after Ella's pushback, then "predict before reveal" added from the Codex cold read. Codex's "later papers revisit your earlier predictions" is struck from v1 (see localStorage constraint).*
3. **Profile captures learning signals, not learning styles.** Domains you know well (analogy sources), default depth, things to skip, and an explicit "already know this" concept list. Filled by a short survey; a plain editable file. "Learns over time" is deferred until there is usage data. Basis: learning-style matching has near-zero effect (d ≈ 0.04 across meta-analyses; Pashler et al. 2008; Newton 2015); a 2024 ACL study of GPT-4 style-adapted explanations found no clear comprehension gain.
4. **Every finding traces to a source sentence, and a checker pass is v1.** Single-pass LLM explanations get signs, equations, and numbers wrong (Intuitive Papers' experience). Claims are typed: *finding* (must cite spans; structurally checked, then semantically audited), *background* (outside knowledge the paper assumes; carries a citation string that is **not verified in v1** and renders with an "unchecked" tag), *illustrative* (analogy, labeled as such, never checked). The structural checker's pass status is called **cited**, not verified, because it proves pointing and numbers, not meaning; a flag-only audit pass covers meaning.
5. **Widgets come from a reviewed template library.** The LLM picks a template id and fills parameters, slider ranges, and labels; trusted code implements the math and owns the hard bounds. Generated code is never executed. Every widget must cite the paper's sentence describing the mechanism it models, and the audit judges whether that text supports the template's shape (a two-group comparison never gets a smooth curve). Papers whose mechanism has no supported template get prose only in v1 (the paper's own figures are v1.1). *Revised from "LLM builds self-contained widgets" after the office-hours cold read, and hardened in eng review.*
6. **Personal first, no accounts.** Product questions are later questions.
7. **Every paper leaves concept records.** Lessons tie back to prior papers by exact concept-id match, and only to papers you have actually opened. A cross-paper concept map is the v2 feature this enables.

## Cross-Model Perspective

Office hours, Codex (gpt-6-astra, fresh context, structured summary only): "rediscover the paper before revealing it" (adopted as predict-before-reveal); "let me change a variable and see what breaks" is the core loop; marimo as a shelf-buy (rejected as the player); weekend build with span ids not quotes, template widgets, checker pass, explicit memory (adopted nearly whole).

Eng review, Codex outside voice on the reviewed plan: ten findings, all resolved by decisions 10-15 below. The load-bearing ones: "verified" overclaimed what the structural checker proves (now "cited" plus a semantic audit); a template's shape can imply a relationship the paper never measured (now a mechanism citation plus audit); lessons keyed by paper alone let a second profile overwrite the first (now keyed by paper and profile, with checks stamped by lesson hash); memory recorded generation as reading (now marked read on open); and the plan committed to a widget framework before proving a lesson teaches (now a spike on an unfamiliar paper first).

## Approaches Considered

- **A: Python pipeline + one self-contained HTML file per lesson (CHOSEN).** CLI does survey, extract, generate, audit, check, repair, render. No server. Completeness 7/10.
- **B: Local web app (FastAPI + Vite player, PDF.js highlights, streaming scenes, library view).** Completeness 10/10. Ella chose B, then reversed to A. Deferred: it is the upgrade path once the lesson format is stable across ~5 papers or a non-technical friend needs it. Decision logged (id 5fa11115).
- **C: marimo notebook as the player.** Rejected: notebook layout fights scene navigation and the quote panel.

## Eng Review Decisions (2026-09-13)

Scope: the separate prerequisite LLM call is removed; one structured call emits rung scenes and story scenes. Extraction uses pymupdf4llm for reading order and word boxes.

| # | Finding | Decision |
|---|---------|----------|
| 1 | Full regeneration on retry discards verified work | **1A** targeted repair call keyed by claim, widget, and scene id; merge leaves untouched items byte-identical |
| 2 | Deep lessons can exceed the output cap | **2B** max_tokens at model maximum; `stop_reason == max_tokens` → clear message; prose capped per scene |
| 3 | Widgets are toys with no on-screen label | **3A** `evidence_span_ids`, "illustrative model" badge, cited sentence under the chart |
| 4 | Widget contract duplicated in Python and JS | **4A** one `manifest.json` per template read by both sides |
| 5 | External failures unspecified | **5A** SDK `max_retries=3`, 10-minute timeout, clear messages; render guards; profile errors name the field |
| 6 | Escaping and prompt-injection boundary unstated | **6A** Jinja autoescape; span text is data; injected-instruction test |
| 7 | No committable PDF fixture | **7A** synthetic two-column PDF built in tests with exact expected spans |
| 8 | No eval for the LLM call | **8A** opt-in `pytest -m eval`, 5 runs, rubric ≥ 4/5, last passing lesson committed as fixture |
| 9 | Paper text re-sent on every call | **9A** stable prefix (spans, registry, slugs) marked cacheable and placed first |
| 10 | "Verified" overclaims | **10A** status renamed *cited*; flag-only semantic audit over claims and scene prose |
| 11 | Template shape can outrun the evidence | **11A** `mechanism_span_ids` required; audit judges template fit; `before-after-groups` template added |
| 12 | Lessons keyed by paper only | **12A** `lessons/<profile_name>/` under each paper; `checks.json` carries `lesson_sha256` |
| 13 | Memory records generation as reading | **13A** records on passing check; `read_date` set by `learn open`; regeneration preserves dates |
| 14 | Framework before proof of teaching | **14A** spike: prose-only lesson on an unfamiliar paper, explain it back, write-up in `docs/spikes/` |
| 15 | Four plan bugs | pass condition requires zero failures of any kind and ≥ 1 finding; repair may replace whole scenes; truncation message names the count, not depth; brief mode must contain problem, tried, found, changed |

## Recommended Approach

**A: Python pipeline, HTML lesson files.**

### Pipeline

```
 learn extract <pdf>          learn generate <id>            learn audit <id>        learn check <id>
 ┌──────────────────┐         ┌──────────────────────┐       ┌────────────────┐      ┌─────────────────────┐
 │ pymupdf4llm      │ spans   │ ONE structured call  │lesson │ flag-only LLM  │flags │ rules 1-7, pure     │ checks
 │ reading order +  │───────▶ │ spans+registry+slugs │──────▶│ pass: claims,  │─────▶│ Python + one node   │───────┐
 │ word boxes →     │  .json  │ (cacheable prefix)   │ .json │ prose, widget  │      │ call; pass = zero   │ .json │
 │ sentence spans   │         │ + profile + depth    │       │ template fit   │      │ failures, ≥1 finding│       │
 └──────────────────┘         └──────────────────────┘       └────────────────┘      └─────────────────────┘       │
                                        ▲                                                       │ fail (once)     │
                                        │ learn repair <id>: previous lesson + failure list     │                 │
                                        └──────── replacements keyed by claim/widget/scene id ◀─┘                 │
                                                                                                                  ▼
 learn open <id>  ◀──────  learn render <id>: lesson + checks (hash must match) + profile → lesson.html  ◀────────┘
 sets read_date            autoescape, TEMPLATES[id] wrapper, base64 crops, badges, flags
```

`learn lesson <pdf>` chains extract → generate → audit → check → (repair → audit → check, once) → render → open. Render and open always run; the exit code is the last check's; memory records are written only by a passing check, and `read_date` only by `open`.

### Layout

```
learningtool/
  learn                    # CLI entry (see Commands)
  learningtool/
    schema.py              # Pydantic models below + validators
    profile.py             # load/validate profile.yaml with field-level error messages
    extract.py             # pymupdf4llm page_chunks (words) -> sentence spans with per-line bboxes; crops
    generate.py            # one structured-output call -> LessonDraft; post-fill; truncation guard
    audit.py               # flag-only semantic audit call -> audit flags (feed check/repair)
    check.py               # rules 1-7 -> checks.json (stamped with lesson_sha256); memory upsert on pass
    repair.py              # previous lesson + failures -> replacements by id; merge
    render.py              # lesson + checks + profile -> lesson.html (Jinja autoescape, inline CSS/JS, base64 crops)
    memory.py              # concepts.json read/upsert; read_date on open; tie-back lookup
    prompts/               # lesson.md, audit.md, repair.md (plain text, versioned)
  widgets/
    registry.py            # loads every templates/<id>/manifest.json
    templates/<id>/manifest.json   # params, hard bounds, named outputs, labels — the single contract
    templates/<id>/model.mjs       # exports model(params) -> {output: number}, curve(params, xs), draw()
  player/                  # scene player JS/CSS inlined into every lesson.html
  data/
    profile.yaml           # created by `learn survey`; profile.example.yaml is committed
    papers/<sha256>/
      source.pdf (ignored), spans.json, crops/ (ignored)
      lessons/<profile_name>/{lesson.json, audit.json, checks.json, lesson.html (ignored)}
    memory/concepts.json
  tests/                   # unit, E2E (headless browser on lesson.html), eval (opt-in), fixtures/build_fixture.py
  docs/designs/            # this document
  docs/spikes/             # 001-does-it-teach.md
```

### Commands

`<id>` is the sha256 of the PDF; any unique prefix of 8+ characters is accepted. `--profile <path>` (default `data/profile.yaml`) selects the profile; its `name` field selects the lessons subdirectory.

- `learn survey [--profile]` writes the profile. Any command that needs one errors with "run `learn survey`" if missing; a malformed profile prints the field name and value, never a traceback.
- `learn extract <pdf>` copies the PDF to `data/papers/<sha256>/source.pdf` and writes `spans.json`. Skips if `spans.json` exists unless `--force`. Fails on no text layer.
- `learn generate <id>` writes `lessons/<profile>/lesson.json`. Refuses papers over 150k span characters. Detects `stop_reason == max_tokens` and exits with "lesson too long for one call (N tokens); chunking arrives in v1.1". SDK client: `max_retries=3`, timeout 600 s; on final failure prints the error class and "re-run learn generate <id>".
- `learn audit <id>` runs the flag-only semantic pass and writes `audit.json`: for every finding, "does the claim follow from its cited sentences"; for every scene's prose, "does anything contradict the cited sentences"; for every widget, "does the mechanism text support a relationship of this template's shape". The audit can only raise flags, never clear a structural failure.
- `learn check <id>` writes `checks.json` (with `lesson_sha256`), never modifying `lesson.json`. Exit 0 only when there are zero failures of any kind (claims, widgets, references, audit flags) and at least one `finding`. On exit 0 it replaces this paper's records in `concepts.json` (delete all for `paper_id`, then insert, preserving any existing `created_at` and `read_date`).
- `learn repair <id>` sends the previous `lesson.json` plus the failure list (claim ids, widget ids, scene ids, reasons) and receives replacements keyed by id; whole scenes may be replaced for scene-level failures. Untouched items stay byte-identical.
- `learn render <id>` reads `lesson.json`, `checks.json`, and the profile; refuses a `checks.json` whose `lesson_sha256` does not match ("re-run learn check"); exits 1 if `source.pdf` is missing ("re-run learn extract <pdf>"); applies removals and flags; writes `lesson.html` with `profile.default_depth` baked in.
- `learn open <id>` opens `lesson.html` and sets `read_date` on this paper's concept records if unset.
- `learn list` shows papers, profiles, and unread lessons. With zero papers it prints: `No lessons yet. Run: learn lesson <pdf>`.
- `learn lesson <pdf>` chains the above as drawn in the pipeline.

Progress output (design decision 4A): after extract, print the paper's verbatim title, page count, and span count so the user knows the right file was read. Then one line per stage that updates in place with an elapsed timer: `generate  waiting on model … 2:14`, `audit …`, `check  14 findings cited · 1 widget ok`, `repair (1 of 1) …`, `render`, `open`. Before generate, print once: `safe to Ctrl-C; nothing is written until the reply parses`.

### Data shapes (versioned, Pydantic-validated)

`Scene` is flat, no unions; dicts are lists of named records so the structured-output schema stays inside the API's supported subset (no recursion, no complex enums, `additionalProperties: false`). `feat/scaffold` validates the emitted JSON schema with the SDK before any prompt is written.

```
Profile       {schema_version, name, known_domains[str], default_depth: brief|standard|deep, skip_topics[str], known_concepts[str]}
Span          {id, page, text, bboxes[[x0,y0,x1,y1]]}   # sentence-level, one page per span (a sentence crossing a page or column break is split); text dehyphenated and ligature-folded; one bbox per rendered line
PaperMeta     {title, authors[str], venue?, year?, pages}   # verbatim from PDF metadata and first-page spans; never model-written
LessonDraft   {hook_question, scenes[Scene], widgets[Widget], concepts[ConceptRecord]}        # what the generate call emits
Lesson        LessonDraft + {schema_version, paper_id, profile_name, paper_meta: PaperMeta, prior_links[PriorLink]}   # generate.py post-fills
Scene         {id, role: prereq|before|problem|tried|predict|found|changed, min_depth: brief|standard|deep,
               headline, analogy_line, prose, claims[Claim], widget_id?,
               prediction_question?, prediction_options[str], prediction_answer_index?, reveal_scene_id?, reveal_note?}
               # headline states the mechanism or finding in plain words; analogy_line is the metaphor; both audited as prose.
               # title and close scenes are rendered by the player from Lesson fields, not emitted by the model.
Claim         {id, text, kind: finding|background|illustrative, quote_span_id?, span_ids[str], citation?}
Widget        {id, template_id, params[{name, value}], ranges[{name, lo, hi}], labels[{name, text}],
               task_question, nudges[{name, text}], readout_output,
               evidence_span_ids[str], mechanism_span_ids[str],
               expected_behaviors[{param, param_delta: +|-, output, output_delta: +|-}]}
               # task_question, nudges, and readout_output are model-written and audited; axes, units, defaults, reset live in the manifest.
ConceptRecord {concept_id (slug), name, one_line, scene_id}
PriorLink     {scene_id, concept_id, paper_id, paper_title, read_date, note}
AuditFlags    {schema_version, lesson_sha256, claims[{claim_id, contradicts: bool, reason}],
               scenes[{scene_id, contradicts: bool, reason}], widgets[{widget_id, template_fits: bool, reason}]}
Checks        {schema_version, paper_id, profile_name, lesson_sha256,
               claims[{claim_id, status: cited|failed|unchecked, reason?}],
               widgets[{widget_id, ok, reason?}], scenes[{scene_id, flags[str], widget_removed, reveal_nulled}]}
RepairRequest {lesson: Lesson, failures[{kind: claim|widget|scene, id, reason}]}
RepairReply   {claims[Claim], widgets[Widget], scenes[Scene]}     # replacements keyed by id
```

Statuses live only in `checks.json`; `lesson.json` on disk is the model's output plus post-filled fields, never mutated by `check`. `scenes[]` order is play order. Validators: every `prereq` scene precedes every story scene; unique `Claim.id`, `Scene.id`, `Widget.id`; every `ConceptRecord.scene_id` resolves; a `predict` scene has a question, ≥ 2 options, and a `reveal_scene_id` resolving to a `found` scene with the same `min_depth` (`prediction_answer_index` optional; null means free-form guess); scenes with `min_depth: brief` include at least one each of `problem`, `tried`, `found`, `changed`. The player finds the predict scene whose `reveal_scene_id` equals the current scene id to show right/wrong.

Quotes are whole spans in v1: a finding's "paper's words" is `spans[quote_span_id].text`. Offsets were dropped because a model cannot count characters reliably; sub-sentence quotes are v1.1. `profile.known_concepts` are free-text names listed in the prompt with "do not emit a prerequisite for these".

`concepts.json` is `[{concept_id, name, one_line, paper_id, paper_title, scene_id, created_at, read_date?}]`. A passing check deletes and re-inserts the paper's records, carrying over `created_at` and `read_date`. `generate.py` fills `Lesson.prior_links` by matching draft concept slugs against records from other papers that have a `read_date`.

### Depth dial

Generate `deep` once. Every scene carries `min_depth`. The player filters client-side: `brief` shows brief scenes, `standard` adds standard, `deep` shows all. The dial is a view, not a regeneration; the brief-arc validator guarantees a complete story at every setting.

### Quotes and crops

The "paper's words" for a finding is `spans[quote_span_id].text`; when null, `span_ids[0]`'s text and crop. `render` inlines one base64 PNG crop per cited span (deduplicated) at 110 DPI: the union of the span's `bboxes` padded by 24 pt and clipped to the page, with the highlight drawn from the `bboxes`. Scenes with no `finding` claims render "No paper's words for this rung".

### Widgets

The LLM emits `template_id`, params, ranges, labels, `evidence_span_ids` (the paper's result this toy is next to), `mechanism_span_ids` (the paper's description of the mechanism the template models), and expected behaviors. The player shows an "illustrative model, not this study's measurements" badge and the evidence sentence under every chart. Each template is a directory with `manifest.json` (the single contract: param names, hard bounds, named outputs, labels) and `model.mjs`; `registry.py` loads the manifests, `render` passes each manifest to its `.mjs` at runtime as `TEMPLATES["<id>"] = (() => { ...; return {model, curve, draw}; })(manifest)`. First templates: `threshold-learning-curve`, `before-after-groups`.

### Checker rules (v1)

1. Every `finding` has ≥ 1 existing `span_ids`; `quote_span_id`, if set, is one of them. Every widget's `evidence_span_ids` and `mechanism_span_ids` exist and are non-empty. Otherwise `failed`.
2. Numeric tokens in a `finding`'s `text` appear in its cited spans' `text`. Regex `-?\d[\d,]*(\.\d+)?|-?\.\d+`; thousands separators stripped; trailing zeros stripped only after a decimal point; `.05` equals `0.05`; no unit or word normalization. Otherwise `failed`. "twelve" vs "12" false failures are accepted and documented.
3. `background` claims with a non-empty `citation` → `unchecked`; empty → `failed`.
4. `illustrative` claims → `unchecked`.
5. `template_id` exists in the registry; every param and range name is declared in its manifest; every ranged param has a value with `lo <= value <= hi`; every range is inside the manifest's hard bounds; every referenced output is a manifest output. Failure: `checks.widgets[].ok = false`, scene flagged `widget_removed`, render omits the widget.
6. Every `expected_behavior` holds strictly (`+` means output at `hi` > output at `lo`; equality fails), evaluated by one batched `node` call over all widgets. Failure as rule 5.
7. `reveal_scene_id`, `widget_id`, `ConceptRecord.scene_id` resolve within the lesson; every `prior_links` `(paper_id, concept_id)` resolves to a `concepts.json` record with a `read_date`. Dangling references are recorded (`reveal_nulled`, or the link dropped) and the scene flagged.
8. Audit flags (from `audit.json` for the same `lesson_sha256`): a claim with `contradicts` → `failed`; a scene with `contradicts` → flagged `prose_contradicts`; a widget with `template_fits = false` → as rule 5.

`cited` = structurally checked (pointing and numbers), not meaning. `failed` = rejected. `unchecked` = not checkable by design. A flagged scene renders with a visible marker on the scene and its arc dot; the lesson never renders silently clean.

### Repair

`learn repair` sends `RepairRequest` and merges `RepairReply` by id: replaced claims, widgets, and scenes overwrite their originals; everything else is byte-identical. The repair prompt shares the cacheable prefix with generate and audit. Audit and check run again on the merged lesson; a second failure renders with flags and exits 1.

### Memory

Records are upserted by a passing check and marked `read_date` by `learn open`. Tie-backs only reference papers with a `read_date`, so the tool never claims you read something you only generated. `learn list` shows unread lessons.

### Player

Superseded in detail by the Design Decisions section below (2026-09-13); the office-hours wireframe at `~/.gstack/projects/ellabellae-learningtool/designs/mockup-20260912/wireframe-scene.png` is the structural ancestor, not the reference. In one paragraph: a title scene (hook question, verbatim paper title and authors, depth dial with scene count and minutes, product mark, Start), then scenes in a single reading column with a story arc as top navigation, a mechanism-first headline with the analogy as its subhead, a one-line tie-back under the headline, prose, an optional widget with a task question and an "illustrative model" badge, and a short source line under each finding that opens an evidence drawer (quote, page, crop with zoom, claim list). Predictions lock in and are revealed by the paper's own sentence. A closing scene shows the prediction beside the finding, the concepts added to memory, and the verbatim citation. Sticky footer with Back, Next, and the depth dial at every width. `render` inlines each widget template as `TEMPLATES["<id>"] = (() => { /* file with export stripped */ return {model, curve, draw}; })(manifest);` so two templates in one lesson never collide, and the player dispatches by `template_id`; node tests import the `.mjs` file directly.
### Extraction

pymupdf4llm `to_markdown(page_chunks=True, extract_words=True)` provides reading order across columns and per-word boxes. `extract.py` then: drops header/footer words (top and bottom 6% of page height), joins words into lines and lines into text with end-of-line dehyphenation while recording an offset table per line, splits sentences by regex, maps each sentence's range back to its lines for `bboxes`, splits any sentence that crosses a page, and stops at the first heading matching References/Bibliography. Ligatures are folded by PyMuPDF's text flags. A visual spot-check script renders every span's highlight on its page so extraction is judged by eye on the first real paper.

## Design Decisions (2026-09-13, /plan-design-review)

Outside voices: Codex (bundled CLI) and an independent Claude subagent both read the plan and wireframe. No hard rejections. Three litmus failures confirmed by both: no product identity on the first screen, headlines not scannable, no motion spec. All resolved below.

| # | Finding | Decision |
|---|---------|----------|
| 1 | Lesson opens on the glossary; no product or promise on screen | **1A** title scene: model-written `hook_question` as the display line, the paper's verbatim title, authors, venue, and year always directly under it (from `PaperMeta`, never the model), depth dial with "N scenes · ~M min", a small `learningtool · paper lesson` mark, Start |
| 2 | Scene headline is the analogy; role is 10px; quote competes with headline | **2A** scene shape: role kicker (16px) → `headline` stating the mechanism in plain words → `analogy_line` as subhead → tie-back one-liner → prose → widget; quote set one size below the headline |
| 3 | Permanent evidence column competes with the lesson; empty on rungs | **3A** single lesson column; each finding shows a muted source line (first words of the cited sentence, page, Cited chip); clicking opens a right-side evidence drawer (full quote, crop with zoom, claim list, "Not shown" list); no drawer on scenes without findings |
| 4 | Terminal silent for 2-8 minutes | **4A** staged progress lines with elapsed timer, title echo after extract, safe-to-cancel note (see Commands) |
| 5 | Failed claims: shown, struck, or hidden? | **5A** removed from the scene; 4px amber left border plus a collapsible strip: "Checker removed 1 claim: number 12% not found in the cited sentence. Read with care."; arc dot gets a hollow amber ring; removed claims listed with reasons under "Not shown" in the drawer |
| 6 | Nothing after the last scene | **6A** closing scene: your prediction beside the paper's finding, the concept records this lesson added ("You now have: …"), the verbatim citation, Back to start; the arc gets a final dot |
| 7 | Eight remaining state cells | accepted: `widget_removed` renders one muted line in the widget's place; audit-skipped banner is one non-dismissible line at the top of every scene; free-form predictions reveal through `reveal_note`; depth switch keeps the current scene or lands on the nearest earlier visible one and announces it, predict/reveal move together, hidden dots collapse, "N of M · ~X min" recomputes; prose-contradicts uses the same amber strip; first paper ever shows no tie-back line; private browsing keeps Next gated on a choice, persists nothing, warns nothing; `learn list` empty state prints the next command |
| 8 | Predict-to-reveal specified as a boolean | **8A** predict scene: options as large selectable rows, "Lock it in →" disabled until chosen, "Skip prediction" as a quiet link; after lock the rows go inert and Back does not unlock. Found scene: "You said: …" at top, then the cited sentence fades in over 150 ms as the answer, then a generous verdict ("Close. The paper found…" / "That's what they found."; never "wrong", never red), then the prose |
| 9 | Widget has no learning task; helper copy has no owner | **9A** model writes `task_question`, per-param `nudges`, and `readout_output` (audited); manifest owns axis labels, units or "normalized", defaults, reset; player shows the question above the chart, a starting-value marker, a reset button, a live readout line ("final alpha power: 0.62 → 0.41"); chart updates are instant, never animated |
| 10 | No design system | **10A** extend the Atlas family; tokens below; DESIGN.md generated from them in `feat/scaffold` |
| 11 | No responsive or accessibility spec | accepted; spec below |
| 12 | Six implementer-facing ambiguities | accepted defaults; register below |

### Interaction states

```
FEATURE            | LOADING                         | EMPTY                                   | ERROR                                        | SUCCESS                                  | PARTIAL
-------------------|---------------------------------|-----------------------------------------|----------------------------------------------|------------------------------------------|----------------------------------------
learn lesson (CLI) | staged lines + timer (4A)       | —                                       | messages per 5A (eng review)                 | browser opens on the title scene         | "repair (1 of 1)" line
Title scene        | —                               | —                                       | —                                            | hook + verbatim paper title + dial       | —
Story scene        | —                               | single column, no frame (3A)            | amber strip + border + hollow arc dot (5A)   | scene per 2A                             | prose-contradicts strip (7e)
Evidence drawer    | —                               | not offered on scenes without findings  | removed claims under "Not shown" with reason | quote, page, crop with zoom, claim chips | Background chips read "unchecked"
Widget             | —                               | one muted line: "Interactive model removed: <reason>" | node math error → treated as removed (check) | question, marker, readout, reset         | —
Prediction         | —                               | —                                       | storage blocked: works in-session, no warning| lock-in; reveal by the quote (8A)        | null answer index → reveal_note
Depth dial         | —                               | —                                       | —                                            | filter; counts and minutes recompute     | current scene hidden → nearest earlier, announced
Tie-back           | —                               | first paper: no line at all             | —                                            | one-liner under the headline             | —
Closing scene      | —                               | —                                       | —                                            | prediction vs finding, concepts, citation | skipped prediction → "You skipped the prediction"
Audit skipped      | —                               | —                                       | one line at the top of every scene           | —                                        | —
learn list (CLI)   | —                               | "No lessons yet. Run: learn lesson <pdf>" | —                                          | table of papers and profiles             | unread marker
```

### Journey storyboard

```
STEP | USER DOES                        | USER FEELS                  | SPECIFIED BY
1    | learn lesson paper.pdf           | anticipation, then doubt    | 4A progress lines, title echo
2    | title scene                      | "oh, a question"            | 1A hook + verbatim title
3    | prerequisite rungs               | oriented                    | 2A hierarchy; rungs are short by design
4    | problem / tried                  | curiosity                   | 2A mechanism first, analogy second
5    | widget                           | agency                      | 9A task question, readout, badge, evidence row
6    | predict                          | stakes                      | 8A lock-in, skip
7    | found                            | payoff or surprise          | 8A reveal by the quote, generous verdict
8    | changed                          | closure of the argument     | prose
9    | closing scene                    | "I got it", ownership       | 6A
```

### Design tokens (the Atlas family, decision 10A)

- Ground `--paper: #F7F3EC` (ivory); text `--ink: #1F1D1A`; muted `--slate: #5B5F66` (≥ 4.5:1 on paper); accent `--terracotta: #B5502F` (active arc dot, lock-in button, focus ring, reward-threshold line); flags `--amber: #B8860B` with a `#FFF4D6` strip; rules `--hairline: #D9D2C5`.
- Type: Instrument Serif for the hook, headline, quote, and verdict; Inter for body, UI, and chips; a mono only for readout numbers. Scale: 16 (chips, kicker, source lines), 18 (body, prose measure 68ch), 22 (analogy line, quote), 28 (headline), 40 (hook on the title scene). Line height 1.55 body, 1.2 display. Nothing below 16px.
- Spacing: 8px grid; scene padding 32px; more space above a heading than below it (48 / 16).
- Depth: one shadow only, on the open drawer, offset 0 8px, blur 24px, 12% ink; no other shadows, no gradients, no icons in circles, no cards except the widget enclosure (1px hairline, 4px radius).
- Motion: scene transition 150 ms ease-out crossfade; reveal fade 150 ms; drawer slide 200 ms; chart updates instant; all disabled under `prefers-reduced-motion`.
- Browser surfaces themed: selection and caret in terracotta, focus ring 2px terracotta with 2px paper offset, scrollbar thin in slate.

### Responsive and accessibility (decision 11)

- ≥ 1024px: single column at 68ch centered; evidence drawer slides in from the right at 420px; arc labels visible per the overflow rule.
- 768-1023px: same column; drawer becomes a bottom sheet; arc labels only for previous, current, next.
- < 768px: arc collapses to "scene N of M" plus a menu button opening the scene list; drawer is a full-screen sheet; footer sticky with Back, Next, and the dial as a segmented control; sliders full width; every target ≥ 44px.
- Footer is `position: sticky` at every width; scenes may scroll.
- Keyboard: ← → for Back/Next; Enter locks a prediction; focus moves to the scene heading on every scene change; arc dots are buttons with `aria-current` on the active one; depth dial is a radio group; sliders are native range inputs with visible labels and values; the drawer traps focus and closes on Esc; visited tie-back links distinct.
- `prefers-reduced-motion` disables the reveal fade and scene transitions.

### Decision register (decision 12)

| Ambiguity | Rule |
|-----------|------|
| Arc past 8 scenes at desktop | labels for first, previous, current, next, last; others on hover/focus; never more than 12 dots, extra scenes grouped under one dot per role |
| Crop geometry | max-height 220px, scaled to fit width, click to zoom to 100% in the drawer; highlight scales with the image |
| Tag matrix | one chip style; text Cited / Background (unchecked) / Analogy; removed claims get no chip and appear under "Not shown" |
| Time-remaining estimate | words ÷ 200 per scene + 60 s per widget + 45 s per prediction, rounded to the minute, recomputed per depth |
| Long paper titles in the header | first 60 characters with an ellipsis; full verbatim title on the title and closing scenes and in the drawer |
| Tie-back link target | plain text plus the read date; becomes a link only when the prior lesson.html exists at render time |

### Design NOT in scope

- A distinct learningtool visual identity via `/design-consultation`: deferred; the Atlas family was chosen deliberately (10A). Revisit if the tool is shared beyond friends.
- Dark mode: the use scene is reading on a laptop or tablet; a dark theme is a token swap later, not a v1 decision.
- Animated widgets (curve morphing between slider positions): rejected in 9A; instant updates keep sliders honest.
- Printable or PDF export of a lesson: not asked for; the HTML file already travels.

### Design: what already exists

- The office-hours wireframe (structure only) and the Atlas mockup seeds at `~/.gstack/projects/ellaetchandy/designs/mockup-20260828/` (type and color family).
- Native `<input type="range">`, `<button>`, radio groups, and `<dialog>` for the drawer: reuse platform controls before writing any custom widget.

### Approved Mockups

| Screen/Section | Mockup Path | Direction | Notes |
|----------------|-------------|-----------|-------|
| Title scene | `~/.gstack/projects/ellabellae-learningtool/designs/mockup-20260913/01-title.png` | hook question in Instrument Serif, verbatim paper title and authors beneath, depth dial with counts and minutes, Start | decision 1A; paper meta never model-written |
| Story scene with evidence drawer open | `~/.gstack/projects/ellabellae-learningtool/designs/mockup-20260913/02-scene-drawer.png` | single column, mechanism headline, analogy subhead, tie-back line, source lines under findings, widget with task question, badge, readout, reset; drawer with quote, crop, chips, "Not shown" | decisions 2A, 3A, 9A, tokens |
| Prediction and reveal | `~/.gstack/projects/ellabellae-learningtool/designs/mockup-20260913/03-predict-reveal.png` | selectable rows, "Lock it in", "Skip prediction"; found scene restates the guess, the cited sentence is the answer, generous verdict | decision 8A |
| Phone width, flagged scene | `~/.gstack/projects/ellabellae-learningtool/designs/mockup-20260913/04-phone-flagged.png` | collapsed arc, amber strip, sticky footer with segmented dial | decisions 5A, 11 |
| Lesson scene (ancestor, 2026-09-12) | `~/.gstack/projects/ellabellae-learningtool/designs/mockup-20260912/wireframe-scene.png` | rough greyscale wireframe from office hours | superseded; kept for lineage only |

### Design Implementation Tasks

- [ ] **D1 (P1, human: ~2 hrs / CC: ~10 min)** — schema — title and close roles rendered by the player; `hook_question`, `PaperMeta`, `headline` + `analogy_line`, `reveal_note`, widget `task_question` / `nudges` / `readout_output`; validators
  - Surfaced by: decisions 1A, 2A, 6A, 7c, 9A
  - Files: `learningtool/schema.py`, `learningtool/extract.py` (PaperMeta), `learningtool/prompts/lesson.md`
  - Verify: schema tests; audit covers the new prose fields
- [ ] **D2 (P1, human: ~2 days / CC: ~1 hr)** — player — title scene, mechanism-first hierarchy, single column + drawer, lock-in and quote-as-reveal, closing scene, amber strips, depth-switch cursor rule, sticky footer, three viewport rules, keyboard and ARIA, reduced motion, tokens
  - Surfaced by: decisions 1A-3A, 5A-8A, 11, 12
  - Files: `player/player.js`, `player/player.css`, `learningtool/render.py`, `tests/e2e/`
  - Verify: E2E flows in the test plan; axe check on the fixture lesson at three widths
- [ ] **D3 (P1, human: ~2 hrs / CC: ~10 min)** — cli — staged progress output with timer, title echo, safe-to-cancel note, `learn list` empty state
  - Surfaced by: decisions 4A, 7h
  - Files: `learn`, `learningtool/progress.py`
  - Verify: snapshot test of the progress lines with a mocked slow model
- [ ] **D4 (P2, human: ~1 hr / CC: ~5 min)** — design system — `DESIGN.md` from the tokens above
  - Surfaced by: decision 10A
  - Files: `DESIGN.md`, `player/player.css`
  - Verify: contrast check on every token pair used for text
- [ ] **D5 (P2, human: ~2 hrs / CC: ~10 min)** — widgets — manifest owns axes, units, defaults, reset; player renders question, starting marker, readout
  - Surfaced by: decision 9A
  - Files: `widgets/templates/*/manifest.json`, `player/widgets.js`
  - Verify: directional test plus a readout snapshot
- [ ] **D6 (P2, human: ~1 hr / CC: ~15 min)** — wireframe — redraw the reference wireframe to match decisions 1-12 at desktop and phone widths
  - Surfaced by: post-pass mockup update
  - Files: `~/.gstack/projects/ellabellae-learningtool/designs/`
  - Verify: by eye, before `feat/player` starts

## NOT in scope

- **Paper figures** (`Figure` records, caption linking, figure fallback): v1.1; trigger and start-here in TODOS.md.
- **Sub-sentence quotes**: v1.1; model-emitted offsets are unreliable, substring matching is the path.
- **Chunking for papers over 150k characters**: v1.1; the truncation message is the trigger.
- **Approach B web app** (streaming, PDF.js, accounts-free sharing to non-Python friends): decision 5fa11115.
- **OCR, Socratic deep mode, automatic preference learning, concept-map UI**: v2.
- **Packaging and releases** (PyPI, binaries): `pip install -e .` from a clone is the only distribution in v1.
- **Nightly eval in CI**: rejected in 8A; the eval is opt-in and local.
- **Fitting template parameters to the paper's data points**: rejected in 3C; the badge and evidence row are the honest v1.

## What already exists

- **Atlas (`~/atlas`)**: the files-are-truth, CLI-is-the-pen pattern. Reused as a shape, not as code (nothing importable).
- **Anthropic SDK structured output** [Layer 1]: `messages.parse()` with a Pydantic model; retries and timeouts built in. Reused; nothing hand-rolled.
- **pymupdf4llm** [Layer 1]: multi-column reading order and word boxes. Reused; the hand-rolled column clustering from the office-hours draft was removed.
- **Anthropic prompt caching** [Layer 1]: cacheable prefix. Reused.
- **PyMuPDF** [Layer 1]: crops, text-layer detection, and the synthetic fixture PDF.
- Nothing in the plan rebuilds an existing capability.

## Failure modes

| Codepath | Realistic failure | Test | Handling | User sees |
|----------|------------------|------|----------|-----------|
| extract | scanned PDF, no text layer | yes | guard, exit 1 | "no text layer; OCR unsupported" |
| extract | column order wrong on unusual layout | fixture only | spot-check script | wrong sentence order in quotes; judged by eye on real papers |
| generate | API 429/529/timeout | mock | SDK retries ×3, then message | "re-run learn generate <id>" |
| generate | output truncated | mock | stop_reason guard | "lesson too long (N tokens)" |
| generate | model ignores span ids | eval | rule 1 fails → repair | flagged scene if repair fails |
| audit | audit call fails | mock | treated as no flags, warning printed | lesson proceeds with "audit skipped" banner |
| check | node missing | yes | clear error | "install Node 20+" |
| repair | replacement ids don't match | yes | merge rejects unknown ids, keeps original | flagged scene |
| render | source.pdf missing (fresh clone) | yes | guard, exit 1 | "re-run learn extract <pdf>" |
| render | stale checks.json | yes | hash mismatch guard | "re-run learn check" |
| player | localStorage blocked | E2E | try/catch | predictions don't persist, no crash |
| memory | regeneration | yes | dates preserved | tie-backs stay truthful |

No critical gaps: every failure mode has handling and a visible message; the one untested path (unusual layouts) is covered by the manual spot-check by design.

## Worktree parallelization

| Step | Modules touched | Depends on |
|------|----------------|------------|
| 1 scaffold | learningtool/schema, profile, learn, tests/, CI | — |
| 2 extract | learningtool/extract, tests/fixtures | 1 |
| 3 player | player/, learningtool/render, tests/ | 1 (fixture lesson.json) |
| 4 widgets | widgets/, tests/ | 1 |
| 5 generate + repair | learningtool/generate, repair, prompts/ | 1, 2 |
| 6 audit + check | learningtool/audit, check, memory | 1, 4, 5 |
| 7 spike | docs/spikes | 5 |
| 8 memory + list + open | learningtool/memory, learn | 6 |

Lanes: `A: 1 → 2 → 5 → 7` (pipeline), `B: 3` (player, independent after 1), `C: 4` (widgets, independent after 1). Launch B and C in parallel worktrees once 1 merges; 6 waits for A and C; 8 waits for 6. Conflict flag: lanes A and B both touch `learningtool/render` only if lane A adds render changes; keep render in lane B.

## Implementation Tasks

Synthesized from this review's findings. Run with Claude Code or Codex; checkbox as you ship.

- [ ] **T1 (P1, human: ~1 day / CC: ~30 min)** — schema — Scaffold package, CLI, Pydantic models with validators, per-profile lesson dirs, `lesson_sha256` in checks
  - Surfaced by: Step 0 scope, 12A, 15d
  - Files: `learningtool/schema.py`, `learn`, `pyproject.toml`, `.gitignore`, `.github/workflows/test.yml`
  - Verify: `pytest tests/test_schema.py`; SDK schema validation passes
- [ ] **T2 (P1, human: ~2 days / CC: ~45 min)** — extract — pymupdf4llm-based sentence spans with bboxes; text-layer guard; crops; synthetic fixture builder
  - Surfaced by: Step 0 scope, Tests 7A
  - Files: `learningtool/extract.py`, `tests/fixtures/build_fixture.py`, `tests/test_extract.py`
  - Verify: exact span assertions on the fixture; spot-check script on the EEG paper
- [ ] **T3 (P1, human: ~1 day / CC: ~30 min)** — generate — one structured call with cacheable prefix, truncation guard, SDK retries/timeout, span-is-data prompt rule
  - Surfaced by: Arch 2B, Quality 5A/6A, Perf 9A, 15c
  - Files: `learningtool/generate.py`, `learningtool/prompts/lesson.md`, `tests/test_generate.py`
  - Verify: mocked 429×3 and max_tokens cases; prompt-order test
- [ ] **T4 (P1, human: ~4 hrs / CC: ~15 min)** — repair — replacements keyed by claim/widget/scene id; byte-identical merge
  - Surfaced by: Arch 1A, 15b
  - Files: `learningtool/repair.py`, `learningtool/prompts/repair.md`, `tests/test_repair.py`
  - Verify: merge test asserts untouched items are identical
- [ ] **T5 (P1, human: ~1 day / CC: ~30 min)** — check — rules 1-8, `cited` status, strict pass condition, manifest registry, batched node call
  - Surfaced by: Quality 4A, 15a, Tension 10A
  - Files: `learningtool/check.py`, `widgets/registry.py`, `widgets/templates/*/manifest.json`, `tests/test_check.py`
  - Verify: one test per rule including the zero-findings and widget-only-failure cases
- [ ] **T6 (P1, human: ~1 day / CC: ~30 min)** — audit — flag-only semantic pass over claims, prose, widget template fit
  - Surfaced by: Tensions 10A, 11A
  - Files: `learningtool/audit.py`, `learningtool/prompts/audit.md`, `tests/test_audit.py`
  - Verify: mocked audit with an inverted claim → failed; eval run
- [ ] **T7 (P1, human: ~1 day / CC: ~30 min)** — render + player — autoescape, template wrapper, widget badge and evidence row, hash guard, source.pdf guard, depth filter, prediction/reveal, flags
  - Surfaced by: Arch 3A, Quality 5A/6A, Tension 12A
  - Files: `learningtool/render.py`, `player/player.js`, `player/player.css`, `tests/test_render.py`, `tests/e2e/`
  - Verify: `<script>` in prose renders as text; two-template lesson; E2E flows
- [ ] **T8 (P2, human: ~3 hrs / CC: ~10 min)** — memory — records on check, `read_date` on open, dates preserved on regeneration, `learn list`
  - Surfaced by: Tension 13A
  - Files: `learningtool/memory.py`, `tests/test_memory.py`
  - Verify: regenerate → read_date unchanged; tie-back excludes unread papers
- [ ] **T9 (P2, human: ~4 hrs / CC: ~20 min)** — eval — opt-in 5-run rubric with injected-instruction case; committed fixture lesson
  - Surfaced by: Tests 8A, Quality 6A
  - Files: `tests/eval/test_lesson_quality.py`, `tests/fixtures/lesson.json`
  - Verify: `pytest -m eval` passes ≥ 4/5
- [ ] **T10 (P2, human: ~1 afternoon / CC: ~1 hr)** — spike — prose-only lesson on an unfamiliar non-BME paper; explain it back; write-up
  - Surfaced by: Tension 14A
  - Files: `docs/spikes/001-does-it-teach.md`
  - Verify: the write-up exists and names method, result, limitations from memory
- [ ] **T11 (P2, human: ~2 days / CC: ~1 hr)** — tests — every GAP in the coverage diagram (30 code paths, 14 user flows)
  - Surfaced by: Test review
  - Files: `tests/`
  - Verify: coverage report; E2E suite green
- [ ] **T12 (P3, human: ~1 hr / CC: ~5 min)** — profile — field-level validation messages; example profile
  - Surfaced by: Quality 5A
  - Files: `learningtool/profile.py`, `data/profile.example.yaml`
  - Verify: malformed profile test asserts the message names the field

## Success Criteria

- `learn lesson <pdf>` on the EEG upper-alpha neurofeedback paper produces a lesson that opens in a browser, and Ella can say what the paper did, why, and its key knobs in ≤ 15 minutes without opening the PDF.
- The spike write-up (T10) shows a prose-only lesson taught an unfamiliar paper well enough to explain method, result, and limitations from memory.
- Every "paper's words" quote is verbatim from the PDF (spot-checked against the page crop).
- At least one scene has a working widget whose slider changes the chart in the direction the paper implies, with the badge and evidence sentence visible.
- The prediction scene appears before the result scene and reveals the paper's finding after a guess.
- A second paper's lesson shows at least one tie-back to the first, naming its title and read date, and only after the first was opened.
- A second profile (`--profile other.yaml`) yields different prerequisite rungs for the same paper, in its own lessons directory. Friends running it is v1.1.

## Distribution Plan

Git clone plus `pip install -e .`; `learn` on PATH; `ANTHROPIC_API_KEY` in the environment; Node 20+. Lessons are single HTML files, so any finished lesson can be sent to anyone. CI is one GitHub Actions workflow running `pytest` (unit and E2E; eval excluded) on push. Deployment is deferred to Approach B.

## Next Steps

Each is its own feature branch, merged to main when green. Order follows the parallelization table.

1. `feat/scaffold` (T1, T12)
2. `feat/extract` (T2), then `feat/player` (T7 render + player on the fixture lesson) and `feat/widgets` (T5's registry + first two templates) in parallel worktrees
3. `feat/generate` (T3, T4)
4. `spike/does-it-teach` (T10) — one afternoon, before anything below
5. `feat/audit-check` (T5, T6)
6. `feat/memory` (T8)
7. `feat/eval` (T9) and `feat/tests` (T11) as the last lake

## The Assignment

Before any code: pick the one paper in Zotero you already understand best (the EEG upper-alpha neurofeedback paper is the candidate). Open it and mark by hand the three sentences you would want the lesson to quote, the one or two parameters you would want on a slider, the sentence that describes the mechanism those parameters belong to, and the two concepts it assumes that a friend outside BME would need first. Then pick one paper outside BME you have not read; that is the spike paper.

## What I noticed about how you think

- You didn't accept "one interactive page per paper." You said "is this just like a summary document then? I want to avoid that," and then described exactly what you wanted instead, with the GPT-4 report as the worked example. Most people agree with the premise and get a summarizer.
- You asked to see the learning-styles evidence before accepting a premise built on it: "I want to know more about the research you found." Then you changed the survey's purpose based on what it said.
- In eng review you stopped a decision three times to ask how the model actually works, for a state diagram, and for the checker rules, before choosing. You chose the recommended option every time, but only after you could draw it yourself.
- "Think of it like a story. Each research paper can be conveyed like a storyline with a plot." That sentence is the product.

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 0 | — | — |
| Codex Review | `/codex review` | Independent 2nd opinion | 0 | — | — |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 1 | CLEAR (PLAN) | 51 issues (7 sections + 44 test gaps), 0 critical gaps, scope reduced |
| Design Review | `/plan-design-review` | UI/UX gaps | 1 | CLEAR (FULL) | score: 4/10 → 9/10, 12 decisions, 0 hard rejections |
| DX Review | `/plan-devex-review` | Developer experience gaps | 0 | — | — |

- **CROSS-MODEL:** Eng review: Codex outside voice raised 10 findings, all resolved (decisions 10-15). Design review: Codex and a Claude subagent both read the plan and wireframe; no hard rejections; three litmus failures confirmed by both (first-screen identity, scannable headlines, motion) and resolved by design decisions 1, 2, 8, and the tokens block. Office-hours cold read was also Codex.
- **VERDICT:** ENG + DESIGN CLEARED — ready to implement.

NO UNRESOLVED DECISIONS
